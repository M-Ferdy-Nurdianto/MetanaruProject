{
  "name": "metanaru-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && mkdir -p ../dist && cp -r dist/* ../dist/",
    "dev": "vercel dev"
  }
}